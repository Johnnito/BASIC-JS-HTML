/*
 * Binary/Arithmetic operatos: + - * /
 *      They can be used for numbers or strings
 * Unary operators: !
 * Asignation: =
 * Comparator: == < > <= != ===
 *      === Exactly the same: 3 === "3" -> false | Compare value and its type
 *      == 3 == "3" -> true | Only compares their types
 * Conditionals: && ||
 * 
 * Increment: var_name++
 * Increment by a number: var_name += 2
 */